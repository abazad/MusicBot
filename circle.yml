machine:
  python:
    version: 3.5.0
    
dependencies:
  pre:
    - sudo apt-get install libssl-dev libffi-dev libasound2-dev libav-tools -y
    
  post:
    - python prep_tests.py
    
test:
  post:
    - python post_tests.py